<ContentDialog
    x:Class="PhotoLib.BrowsePics"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoLib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Open"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_OpenClick"
    SecondaryButtonClick="ContentDialog_CancelClick">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="Image"
            Width="206"
            Height="38"
            Margin="10,10,0,0"
            PlaceholderText="Browse To Select Pictures"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>
            <Button x:Name="Browse"
            Width="88"
            Height="38"
            Margin="10,10,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Content="Browse"
            Click="Browse_Click" />
        </StackPanel>
    </Grid>
</ContentDialog>
