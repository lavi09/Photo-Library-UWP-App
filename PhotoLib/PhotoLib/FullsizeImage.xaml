<Page
    x:Class="PhotoLib.FullsizeImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoLib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Button Name="Back" Content="Back"  Click="Back_Click" HorizontalAlignment="Left" Height="92" Margin="10,0,0,0" VerticalAlignment="Center" Width="87" RenderTransformOrigin="2.734,-1.125" />
        <FlipView x:Name="flipView1" Width="480" Height="270" 
          BorderBrush="Black" BorderThickness="1">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <!--<Image
                        Width="480" Height="270" Stretch="UniformToFill"
                       Source="{x:Bind collection}"/>-->

                    </Grid>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
    </Grid>
</Page>